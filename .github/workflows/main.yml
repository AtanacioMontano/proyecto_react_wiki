name: Notify on Push to Branch

on:
  push:
    branches: [ "main" ]  # Cambia a tu rama deseada (develop, feature/*, etc.)

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Notify via Slack/Discord/Email
        run: |
          echo "📢 ¡Se hizo push a la rama ${{ github.ref }}!"
          
